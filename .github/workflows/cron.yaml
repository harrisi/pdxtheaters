name: daily-cron
on:
  schedule:
    - cron: '13 13 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Get showtimes
        run: |
          curl -v --url 'https://www.pdxtheaters.com/api/cron' \
          --header 'Authorization: Bearer ${{ secrets.API_SECRET_KEY }}'
